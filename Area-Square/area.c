#include<stdio.h>
#include<conio.h>
int main()
{
    float len, area;
    printf("Enter one side of the Square: ");
    scanf("%f", &len);
    areas = len*len;
    printf("\nArea = %0.2f", area);
    getch();
    return 0;
}
