#include<stdio.h>
#include<conio.h>
int main()
{
    float side, area;
    printf("Enter the lenght of one side of the Square: ");
    scanf("%f", &side);
    areas = side*side;
    printf("\nArea = %0.2f", area);
    getch();
    return 0;
}
